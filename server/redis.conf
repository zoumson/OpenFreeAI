# Minimal Redis config for development

# Bind to all interfaces (Docker container networking)
bind 0.0.0.0

# Disable protected mode in Docker
protected-mode no

# Save DB every 60 seconds if at least 1 key changed
save 60 1

# Log to stdout so Docker logs capture it
logfile ""
